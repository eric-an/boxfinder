<div class="container-fluid">

<h1>Listing Boxes</h1>
  <div class="new_shipment_link">
    <%= link_to 'Create New Shipment', shipment_path(@shipment), class: "btn btn-lg btn-primary btn-block" %><br>
  </div>
  
  <div class="row box_section">

    <h4>Box Location</h4>

    <div class="col-xs-3 col-s-3 col-md-1 box_section_text">
      <h6>Section A</h6>
      <% Box.box_location("A").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>

    <div class="col-xs-3 col-s-3 col-md-1 box_section_text">
      <h6>Section B</h6>
      <% Box.box_location("B").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>

    <div class="col-xs-3 col-s-3 col-md-1 box_section_text">
      <h6>Section C</h6>
      <% Box.box_location("C").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>

    <div class="col-xs-3 col-s-3 col-md-1 box_section_text">
      <h6>Section D</h6>
      <% Box.box_location("D").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>

    <div class="col-xs-3 col-s-3 col-md-1 box_section_text">
      <h6>Section E</h6>
      <% Box.box_location("E").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>

    <div class="col-xs-3 col-s-3 col-md-1 box_section_text">
      <h6>Section F</h6>
      <% Box.box_location("F").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>

    <div class="col-xs-3 col-s-3 col-md-1 box_section_text">
      <h6>Section G</h6>
      <% Box.box_location("G").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>

    <div class="col-xs-3 col-s-3 col-md-1 box_section_text">
      <h6>Section H</h6>
      <% Box.box_location("H").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>

    <div class="col-xs-3 col-s-3 col-md-1 box_section_text">
      <h6>Section I</h6>
      <% Box.box_location("I").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>
  </div><!-- /row -->

  <div class="row box_section_text_two">
    <div class="col-xs-3 col-s-3 col-md-offset-4 col-md-2">
      <h6>Double-Walled</h6>
      <% Box.box_location("DW").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>

    <div class="col-xs-3 col-s-3 col-md-2">
      <h6>Large Boxes</h6>
      <% Box.box_location("LRG").each do |box| %>
        <p><%= box.length %> x <%= box.width %> x <%= box.height %></p>
      <% end %>
    </div>
  </div><!-- /row -->  

  <div class="row box_list">
    <h4>Box Index</h4>
    <div class="col-xs-3 col-s-3 col-md-offset-3 col-md-2">
      <strong>Name</strong>
    </div>
    <div class="col-xs-3 col-s-3 col-md-2">
      <strong>Dimensional Weight</strong>
    </div>
    <div class="col-xs-3 col-s-3 col-md-3">
      <strong>Other info.</strong>
    </div>
    <div class="col-xs-3 col-s-3 col-md-2">
    </div>
  </div><!-- /row -->

  <%= render @boxes %>

<br>
</div>

